<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PokerGuess.Views.GamePage"
             xmlns:views="clr-namespace:PokerGuess.Views"
             Padding="5,15,5,5">
    <ContentPage.Content>
        <StackLayout x:Name="MainTableLayout"
                     BackgroundColor="ForestGreen" 
                     Padding="10" 
                     Orientation="Vertical" >

            <views:TableContentView x:Name="TableCV" VerticalOptions="EndAndExpand" />
            <Label BackgroundColor="White" LineBreakMode="WordWrap" FontSize="Small"
                     VerticalOptions="EndAndExpand"
                   Text="{Binding HandsInfoText}" />
            <StackLayout VerticalOptions="End" Orientation="Horizontal" HorizontalOptions="FillAndExpand">
                <Button Text="{Binding MainButtonText}" 
                        Command="{Binding MainButtonCommand}" 
                        BackgroundColor="{Binding MainButtonBkgColor}" 
                        Margin="10"
                        HorizontalOptions="FillAndExpand"/>
            </StackLayout>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>